#pragma once
#include "../../renderer/color.hpp"

namespace menu::framework {
    namespace colors {
        const auto background = Color( 25, 25, 25 );
        const auto outline = Color( 15, 15, 15 );
        const auto topbar = Color( );
        const auto highlight = Color( 255, 255, 255 );
    }

    namespace constants {
        constexpr auto font_size = 16.f;
    }
}
